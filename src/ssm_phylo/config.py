"""Runtime config loading (merging chain: env < default.yaml < override.yaml < CLI)."""


def main() -> int:
    """Placeholder: config loader (YAML merge + CLI overrides) lives here."""
    print("ssm_phylo.config: config loading/merging (Phase 1).")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
