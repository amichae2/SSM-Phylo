# ---- python ----
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# ---- tooling caches ----
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# ---- virtualenvs ----
.venv/
venv/
env/

# ---- secrets (local env; .env.example is the template) ----
.env

# ---- generated checkpoints / weights ----
*.pt
*.ckpt
*.safetensors
*.bin

# ---- local scratch / results (paths are normally env-driven; keep the repo clean) ----
# Root-anchored ONLY: a bare `data/` pattern matches ANY directory named data,
# including src/ssm_phylo/data/ — that silently excluded the entire data
# package from git once before. Do not revert to the bare pattern.
/data/
!src/ssm_phylo/data/
ckpts/
checkpoints/
results/
logs/
nohup.out

# ---- vendored third-party simulation code (copied at runtime from PHYLOFORMER_DIR) ----
scripts/vendor/

# ---- editors / OS ----
.idea/
.vscode/
.DS_Store

# ---- experiment tracking local dirs ----
wandb/
runs/
